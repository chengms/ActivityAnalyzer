# 运行应用指南

## 🚀 直接运行窗口模式

### 方法 1: 使用 npm start（推荐）
```bash
npm start
```

### 方法 2: 使用 electron 命令
```bash
npx electron .
```

### 方法 3: 完整构建后运行
```bash
# 构建主进程
npm run build:main

# 构建渲染进程
npm run build:renderer

# 运行
npm start
```

---

## ⚠️ 如果应用卡住或无法启动

### 问题 1: better-sqlite3 编译失败
**症状：** 应用启动时卡住，没有窗口显示

**解决方案：**
1. 检查是否安装了 Visual Studio Build Tools
2. 或者使用预编译版本：
   ```bash
   npm install better-sqlite3 --build-from-source=false
   ```

### 问题 2: 资源路径错误
**症状：** 窗口显示但页面空白

**已修复：** 已在 `vite.config.ts` 中添加 `base: './'` 使用相对路径

### 问题 3: 应用在后台运行
**症状：** 看不到窗口，但进程在运行

**检查方法：**
- 查看系统托盘（任务栏右下角）
- 检查任务管理器中的 Electron 进程
- 如果设置了"启动时最小化"，窗口会被隐藏

### 问题 4: 端口被占用
**症状：** 开发模式无法启动

**解决方案：**
```bash
# 停止所有 Node 进程
Get-Process | Where-Object {$_.ProcessName -like "*node*"} | Stop-Process -Force
```

---

## 🔍 调试方法

### 查看控制台输出
```bash
# 直接运行并查看输出
npx electron . --enable-logging
```

### 打开开发者工具
在 `src/main/main.ts` 中临时添加：
```typescript
mainWindow.webContents.openDevTools();
```

### 检查构建文件
```bash
# 检查文件是否存在
Test-Path dist\main\main.js
Test-Path dist\renderer\index.html
Test-Path dist\renderer\assets\*
```

---

## 📝 常见问题

### Q: 为什么运行命令会卡住？
A: 可能的原因：
1. better-sqlite3 需要编译，但缺少编译工具
2. 应用已启动但窗口被隐藏（检查托盘）
3. 有错误但没有显示（查看控制台）

### Q: 如何确认应用正在运行？
A: 
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*electron*"}
```

### Q: 如何强制关闭应用？
A:
```powershell
Get-Process | Where-Object {$_.ProcessName -like "*electron*" -or $_.ProcessName -like "*node*"} | Stop-Process -Force
```

---

## ✅ 正常启动的标志

1. **进程运行：** 任务管理器中有 Electron 进程
2. **窗口显示：** 看到应用主窗口
3. **托盘图标：** 系统托盘有应用图标（如果有图标文件）
4. **无错误：** 控制台没有错误信息

---

## 🎯 快速启动脚本

创建 `start.bat` 文件：
```batch
@echo off
echo 正在启动活动分析器...
npm start
pause
```

然后双击运行即可。

